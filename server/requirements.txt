# FastAPI 与 ASGI 服务
fastapi==0.115.6
uvicorn[standard]==0.34.0

# LangGraph / LangChain 生态：Agent 编排、工具调用、模型接入的核心。
# 注意：不引入 langchain 伞包——项目只用到 langgraph / langchain-core /
# langchain-openai，砍掉伞包能大幅减小部署体积（Vercel 函数有 500MB 上限）。
langgraph==0.2.60
langchain-core==0.3.29
langchain-openai==0.2.14

# 环境变量、数据校验
python-dotenv==1.0.1
pydantic==2.10.4

# IP 速率限制与封锁
slowapi==0.1.9
limits==3.13.0
